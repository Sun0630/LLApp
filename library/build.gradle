apply plugin: 'com.android.library'

//def ndkFolder = file(plugins.getPlugin('com.android.library').sdkHandler.getNdkFolder()).absolutePath

android {
    compileSdkVersion 24
    buildToolsVersion "24.0.2"
    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 18
        versionCode 1
        versionName "1.0"
//        ndk {
//            moduleName "heaton"
//            ldLibs "EGL", "GLESv3", "dl", "log"
//            abiFilters "armeabi","x86"
//            stl "gnustl_static"
//            cFlags "-I${ndkFolder}/sources/android/native_app_glue -I${ndkFolder}/platforms/android-19/arch-arm/usr/include"
//        }
    }
//    sourceSets {
//        main {
//            jni.srcDirs = ["src/main/jni"]
//        }
//    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
//            jniDebuggable true
        }
    }
//    externalNativeBuild{
//        ndkBuild {
//            path file('src/main/jni/')
//        }
//    }
    productFlavors {
    }
    packagingOptions {
        exclude 'META-INF/DEPENDENCIES'
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
        exclude 'META-INF/NOTICE.txt'
        exclude 'META-INF/LICENSE.txt'
    }

    task copyAAR(type: Copy) {
        from('build/outputs/aar')
        into('release')
        include(project.name + '-release.aar')
    }

    android.libraryVariants.all { variant ->
        if (variant.name == 'release') {
            bundleRelease.dependsOn copyAAR
        }
    }

}


dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:24.2.1'
    compile 'com.alibaba:fastjson:1.2.7'
    compile 'org.apache.httpcomponents:httpcore:4.4.4'
    compile 'org.apache.httpcomponents:httpmime:4.5.2'
    compile 'com.mcxiaoke.volley:library:1.0.19'
}
